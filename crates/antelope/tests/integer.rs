
#[test]
fn int8() {
    /*
    // Test the `From` trait conversion
    let int8_instance: Int8 = 42.into();
    let uint128_instance: UInt128 = 120000300.into();
    //let int8_fail: Int8 = 200.into(); confirmed fails
    assert_eq!(int8_instance.value, 42);

    // Test the `new` constructor
    let int8_instance = Int8::new(-5);
    assert_eq!(int8_instance.value, -5);

    let primitive_int8: i8 = int8_instance.value + 10;
    assert_eq!(primitive_int8, int8_instance.value + 10);
    assert_eq!(primitive_int8, 5);

    assert_eq!(int8_instance.get_abi_name(), "int8");
    assert_eq!(uint128_instance.get_abi_name(), "uint128");
     */
}
